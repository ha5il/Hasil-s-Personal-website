version: '3.8'

services:
  vue-app:
    image: node:14
    working_dir: /app
    volumes:
      - ./:/app
    ports:
      - "8080:8080"
    command: sh -c "npm install && npm run serve"
    environment:
      - NODE_ENV=development
      - CHOKIDAR_USEPOLLING=true
